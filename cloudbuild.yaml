steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/ghazara-website:$COMMIT_SHA', '.']

  # Push the container image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/ghazara-website:$COMMIT_SHA']

  # Deploy container image to Google Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'ghazara-website'
      - '--image'
      - 'gcr.io/$PROJECT_ID/ghazara-website:$COMMIT_SHA'
      - '--region'
      - 'me-central1' # Saudi Arabia / Middle East region (or us-central1)
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'

images:
  - 'gcr.io/$PROJECT_ID/ghazara-website:$COMMIT_SHA'
