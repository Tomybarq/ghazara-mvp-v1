# Ghazara Website V2 — Baseline Safety & Discovery Audit

**تاريخ التدقيق:** 16 أغسطس 2026  
**الإصدار الخاضع للفحص:** Ghazara MVP V1 (Baseline Commit: `main`)  
**الهدف:** توثيق الحالة الراهنة للمشروع بدقة، ورصد الفجوات الهندسية والمحتوى الحساس، وتصنيف أولويات الإصلاح قبل البدء في تعديلات كود V2.

---

## 1. نتائج أوامر التحقق والتشخيص الأساسية

| الأمر | النتيجة الفعلية | الملاحظات الفنية |
|---|---|---|
| `pnpm check` | **PASS (Exit 0)** | فحص TypeScript سليم عبر كامل المشروع بدون أخطاء `tsc`. |
| `pnpm test` | **PASS (Exit 0)** | 3 ملفات اختبار ناجحة (5 اختبارات) تغطي `rfq.submit`, `auth.logout`, و`Vercel Express bridge`. |
| `pnpm build` | **PASS (Exit 0)** | تم بناء الحزمة (`dist/public` و`dist/index.js`) بنجاح، مع تحذير حجم الحزمة الأحادية (> 530 KB JS غير مقسم). |
| `pnpm run build:vercel` | **PASS (Exit 0)** | تم بناء عميل Vite للإنتاج بنجاح. |
| `git status` | **Clean baseline** | المستودع على فرع `main` جاهز للتطوير المرحلي. |

---

## 2. جدول الفحص والتدقيق الفني الشامل (V1 Audit Matrix)

| الملف / المكون | الوضع الحالي في V1 | المشكلة المرصودة | الأثر التجاري والتقني | أولوية الإصلاح | طريقة التحقق |
|---|---|---|---|---|---|
| `client/src/App.tsx` | يلف المكونات بـ `ThemeProvider` فقط دون `LanguageProvider`، ويستورد كافة الصفحات بشكل متزامن. | غياب مزود اللغة المركزي على مستوى التطبيق؛ عدم وجود Route لـ `/request-quote`؛ غياب Code Splitting. | تجربة مستخدم غير متسقة عند التنقل؛ بطء تحميل أولي؛ فقدان مسار تحويل رئيسي. | **A (Must Fix)** | فحص شجرة الـ Providers وتتبع مسارات URL وقياس حجم الحزمة. |
| `client/src/contexts/LanguageContext.tsx` | موجود لكنه غير مستخدم في `App.tsx` ولا في الصفحات. | كل صفحة تدير لغتها محليًا عبر `useState` معزول. | عند تغيير اللغة في صفحة والانتقال لأخرى تفقد اللغة المحددة؛ تعطل LTR/RTL الموحد. | **A (Must Fix)** | تفعيل `useLanguage` في كل صفحة وتأكيد `document.documentElement` lang/dir. |
| `client/src/components/Header.tsx` & `Footer.tsx` | روابط التنقل مكتوبة كـ Anchors للصفحة الرئيسية (`#hero`, `#about`, `#services`...) بدلاً من Routes حقيقية. | النقر على الروابط من داخل صفحة فرعية مثل `/about` أو `/products` لا ينقل للمسار الصحيح بشكل منظم. | ارتباك الزائر، انهيار بنية الـ Internal Linking لـ SEO، وصعوبة تصفح الموقع متعدد الصفحات. | **A (Must Fix)** | استبدال الروابط بـ Routes حقيقية (`/about`, `/services`, `/products`, إلخ) مع دعم scroll إذا كان في الصفحة الرئيسية. |
| `client/src/components/ui/Logo.tsx` | يعتمد على مسار افتراضي خارجي `/manus-storage/logo-*.webp`. | الصور غير موجودة محلياً في `client/public`، مما يجبر المكون على التراجع للرسم البديل (Fallback SVG). | مظهر غير متناسق للهوية التجارية، وفشل في تحميل الشعار الرسمي للعلامة. | **A (Must Fix)** | إضافة ملفات الشعار الرسمية SVG/WebP محلياً داخل `client/public/assets/branding/`. |
| `client/public/` | لا يحتوي على `robots.txt` ولا `sitemap.xml`. | غياب ملفات التحكم في الزحف والفهرسة لمحركات البحث. | محركات البحث لن تفهرس الصفحات بشكل منظم؛ فقدان الظهور في نتائج البحث. | **A (Must Fix)** | إنشاء `robots.txt` و`sitemap.xml` مطابقين للمسارات الحقيقية. |
| `client/index.html` | يحتوي على `maximum-scale=1`، ومتغيرات تحليلات غير معرّفة `%VITE_ANALYTICS_*%`، ويفتقر لـ OpenGraph/Twitter Meta. | قيود الوصول لذوي الاحتياجات الخاصة (WCAG Zoom Violation)؛ تحذيرات في الـ Build؛ ضعف المشاركة في الشبكات الاجتماعية. | تجربة مشاركة باهتة بدون معاينة (Rich Snippet)؛ مخالفة معايير الـ Accessibility. | **A (Must Fix)** | تصحيح الـ viewport وإضافة علامات SEO وOpenGraph وTwitter Cards. |
| `client/src/data/content.ts` | ملف أحادي ضخم يحتوي على نصوص مكررة وبيانات غير معتمدة. | خلط نصوص الواجهة بالبيانات التجارية الحساسة؛ وجود أرقام هواتف وهمية وادعاءات غير موثقة. | مخاطر نشر معلومات تجارية خاطئة وصعوبة الانتقال لنظام CMS مستقبلاً. | **A (Must Fix)** | فصل المحتوى إلى نماذج ثنائية اللغة (`company.ts`, `services.ts`, `products.ts`, `seo.ts`...). |
| `client/src/components/quote/RFQModal.tsx`, `RfqForm.tsx`, `ContactPage.tsx` | تكرار منطق النماذج في 3 أماكن مختلفة مع سلوكيات متباينة (النموذج يرسل WhatsApp بينما الصفحة ترسل tRPC فقط). | ازدواجية الكود، تباين تجربة التحويل، عدم وجود صفحة مستقلة لطلب عرض السعر `/request-quote`. | تشتت العميل وفقدان Leads بسبب عدم وضوح المسار الموحد. | **A (Must Fix)** | توحيد مكونات ومنطق الـ RFQ وربط صفحة `/request-quote` مع الـ Modal. |
| `server/routers.ts` & `server/db.ts` | معالج `rfq.submit` يتحقق من الحقول باستخدام Zod ويحفظ في قاعدة البيانات بنجاح، لكنه يفتقر لمعدل الطلبات المتقدم (Rate Limiting). | البنية آمنة ونظيفة، ولكن قد تكون عرضة للـ Spam بدون قيود خادمية أو تدقيق إضافي. | احتمال استقبال طلبات غير حقيقية أو استنزاف الموارد. | **B (Should Fix in V2)** | إضافة طبقة معالجة أخطاء آمنة وحماية من التكرار. |
| `client/src/pages/Hub.tsx` & `BioHubModal.tsx` | صفحة وسائط سريعة (Linktree/Hub style) بروابط مباشرة. | تستخدم رقم هاتف مختلف (`+967 784 984 528`) ولا تتبع نفس الـ Layout العام. | تباين في الهوية وتشتت هوية التواصل. | **B (Should Fix in V2)** | مواءمة الـ Hub مع مسارات المؤسسة وربطها بنموذج RFQ والبيانات المعتمدة. |
| `client/src/pages/ComponentShowcase.tsx` | صفحة داخلية كبيرة (59 KB) لعرض المكونات. | محملة ضمن البنية دون الحاجة إليها في الإنتاج التجاري. | زيادة حجم الحزمة دون فائدة للمستخدم النهائي. | **C (Future / Cleanup)** | عزلها أو تطبيق Code Splitting الكامل عليها. |

---

## 3. تدقيق المحتوى الحساس والبيانات المتعارضة (Content Truth & Safety)

> [!CAUTION]
> تم رصد تعارض واضح بين مصادر أرقام الهواتف والتواصل داخل المشروع:
> 1. **`content.ts` (الأساسي):** يعرض `+967 700 000 000` (رقم عام / Placeholder).
> 2. **`types/index.ts`:** يعرّف `WA_PHONE = "967779030679"`.
> 3. **`BioHubModal.tsx` & `todo-phase2.md`:** يعرض رقم `+967 784 984 528`.
>
> **القرار الإجباري:** لا يجوز اعتماد أي من هذه الأرقام ونشره في الإنتاج كحقيقة نهائية قبل توقيع واعتماد صاحب المؤسسة. تم توثيق ذلك فوراً في `CONTENT_REQUIRED.md`.

### عناصر المحتوى غير المعتمدة المرصودة حالياً:
- **العنوان المؤسسي:** `اليمن — حضرموت / صنعاء / عدن` (عام جداً وغير محدد).
- **وعود الاستجابة (SLA):** عبارة `"خلال 24 ساعة"` المذكورة في نصوص الـ RFQ.
- **الروابط الخارجية:** روابط مشاريع خارجية مثل `https://github.com/Tomybarq/moeen-ngo` معروضة كمنتجات تابعة.
- **المدونة:** مقالات عامة نموذجية بدون مؤلفين أو تواريخ نشر موثقة.
- **شركاء النجاح والشهادات:** قطاعات عامة دون أسماء عملاء أو شهادات معتمدة.

---

## 4. تصنيف النتائج حسب الأولوية

### A) Must Fix Before Release (إصلاحات حتمية لـ V2)
1. تفعيل `LanguageProvider` المركزي وتوحيد إدارة RTL/LTR و`lang`/`dir` على مستوى `<html>`.
2. تحويل الـ Navigation في `Header` و`Footer` إلى مسارات حقيقية مستقلة مع الحفاظ على التمرير السلس.
3. إنشاء صفحة `/request-quote` وتوحيد تدفق RFQ مع حماية ضد التكرار وحالات (Loading, Error, Success).
4. إضافة الأساس التقني لـ SEO: `robots.txt`, `sitemap.xml`, OpenGraph, Twitter Cards, وStructured Data (Schema.org).
5. فصل المحتوى والبيانات إلى نماذج TypeScript ثنائية اللغة (`content/` structure) CMS-ready.
6. تثبيت أصول الشعار محلياً داخل `client/public/` بصيغ SVG وWebP خفيفة وعالية الدقة.
7. إنشاء وتفعيل `CONTENT_REQUIRED.md` لحوكمة المحتوى التجاري المعتمد.

### B) Should Fix in V2 (تحسينات أساسية)
1. تطبيق Code Splitting عبر `React.lazy` و`Suspense` لكافة الصفحات لتخفيض الحزمة الأساسية.
2. تحسين التباين والألوان وفقاً لدليل الهوية (Deep Iris `#5E3B95` وHoneyed Amber `#E6833E` وAmethyst `#8A4E91`).
3. مواءمة صفحة `Hub` وربطها بالتدفق العام للموقع.
4. إزالة الرموز والتحذيرات غير المعرّفة في `index.html`.

### C) Future Enhancement (توسعات لاحقة)
1. إضافة صفحات تفاصيل المنتجات والمدونة (`/products/:slug`, `/blog/:slug`) فور توفر محتوى أصلي وكامل.
2. ربط منصات التحليلات الرسمية (Google Analytics / GTM) بعد تزويد المعرفات المعتمدة.
3. دمج نظام إدارة محتوى (Headless CMS) مباشر مع النماذج المنفصلة.

---

## 5. توصية الجاهزية (Go / No-Go)

- **حالة الكود الأساسي:** سليم وقابل للتطوير (Build & Test passing).
- **القرار:** **GO** لبدء **Phase 1 (Content Truth, Brand Assets & Design System)** مع الالتزام التام بعدم إدخال أي رقم أو معلومة غير موثقة.
